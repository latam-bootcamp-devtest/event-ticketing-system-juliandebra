export const getTicketsService = async () => {
  const tickets = "";
  return tickets;
};

export const createTicketService = async () => {
  const newTicket = "";
  return newTicket;
};
export const cancelTicketService = async () => {
  const deleted = true;

  if (!deleted) throw new Error("Error deleting ticket");
};
